import SignInUpForm from "../../components/sections/signInUpForm";

const LoginPage = () => {
  return (
    <div className="flex items-center justify-center min-h-screen bg-gray-100">
      <SignInUpForm />
    </div>
  );
};

export default LoginPage;
